# SearchField

```javascript
  <SearchField
    accessibilityLabel="Demo Search Field"
    id="searchField"
    onChange={({ value }) => console.log({ value })}
    placeholder="Search and explore"
  />
```
