# SearchField

```javascript
      <Box color="white" shape="rounded" padding={3} display="flex" direction="row" alignItems="center" maxWidth={300}>
        <Box flex="grow" paddingX={2}>
            <SearchField
              accessibilityLabel="Demo Search Field"
              id="searchField"
              onChange={({ value }) => console.log({ value })}
              placeholder="Search and explore"
            />
          </Box>
        </Box>
```
